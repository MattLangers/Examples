import type { ProductType } from "$lib/models/productType"

export interface IProductTypeIcon {
    GiveMeImage(productType: ProductType): HTMLElement
}

export class ProductTypeIcon implements IProductTypeIcon{
    GiveMeImage(productType: ProductType): HTMLElement {
        var el: HTMLElement = new 
        return 
        
    }
}